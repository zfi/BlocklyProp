<?xml version="1.0" encoding="UTF-8"?>
<!--
Logging configuration for blockly.parallax.com

Valid logging levels: TRACE, DEBUG, INFO, WARN and ERROR
-->

<configuration>
    <!-- Log file destinations are defined here -->
    
    <!-- <property name="APP_LOG_PATH" value="${catalina.base}/logs"/>  -->
    <property name="APP_LOG_PATH" value="${catalina.base}/logs"/>
    <property name="APP_LOG_FILE_BASE" value="blockly-app"/>
    
    <!--
    <appender name="json" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
            <jsonFormatter
                class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                <prettyPrint>true</prettyPrint>
            </jsonFormatter>
            <timestampFormat>yyyy-MM-dd' 'HH:mm:ss.SSS</timestampFormat>
        </layout>
    </appender>
    -->
    
    <appender name="file" class="ch.qos.logback.core.FileAppender">
        <file>${APP_LOG_PATH}/${APP_LOG_FILE_BASE}-debug.log</file>
        <encoder>
            <pattern>%date %level [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>
    
    <!--  Rolling log files  -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${APP_LOG_PATH}/${APP_LOG_FILE_BASE}.log</file>
     
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>${APP_LOG_PATH}/${APP_LOG_FILE_BASE}.%d{yyyy-MM-dd-HH-mm}.log</fileNamePattern>
            <maxHistory>10</maxHistory>
        </rollingPolicy>
            
        <encoder>
            <!--   <pattern>%relative [%thread] %-5level %logger{35} - %msg%n</pattern>  -->
            <pattern>%date %level [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>   

    <!--
    <logger name="com.parallax.server.blocklyprop.monitoring" level="info">
        <appender-ref ref="RollingFile"/>
    </logger>
    -->
    
    <logger name="com.parallax.server.blocklyprop" level="info">
        <appender-ref ref="file"/>
    </logger>

    
    <!-- 
    <logger name="com.parallax.server.blocklyprop.servelets" level="debug">
        <appender-ref ref="RollingFile"/>
    </logger>
    --> 
    
    <root level="info">
        <appender-ref ref="RollingFile" />
    </root>
</configuration>